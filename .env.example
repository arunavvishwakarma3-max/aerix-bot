BOT_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here

# Owner Discord ID (required for /eval, /broadcast, /servers, /blacklist, /whitelist)
# OWNER_ID=your_discord_user_id_here

# Uncomment for instant guild-specific deployment (dev only).
# Leave commented for global deployment (takes ~1hr to propagate).
GUILD_ID=

# Database: SQLite (stored locally — use Render Disk for persistence)
